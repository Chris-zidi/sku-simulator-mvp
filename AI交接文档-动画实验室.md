# SKU 经营训练模拟器 — AI 开发交接文档

> **写给下一个接手的 AI**：本文档由上一任 AI 于 2026-07-26 编写。请先完整读完本文档，**尤其是第 2 章（用户真正想要什么）和第 6 章（踩过的坑）**，再动手改代码。用户会用真实截图反馈问题，你要先复现、再诊断、再改。
>
> 当前版本：`v0.9.3`（commit `c957895`，已推 GitHub main）

---

## 1. 项目是什么

**一句话**：一个纯前端、零依赖的单页 Web 应用，教一个非开发背景的电商运营新人（用户本人 Chris，就职于 DJI 全球商城配置中台）从 0 学会 **SKU 库存经营 / 销售预测（Forecast）** 的核心概念。

**三个 Tab**：
| Tab | 文件 | 状态 |
|---|---|---|
| 📚 教学训练 | `teach-core.js` + `teach-data.js` | 稳定，近期没动 |
| 🧪 自由沙盘 | `teach-core.js` 内 | 稳定，需通关教学解锁 |
| 🎬 动画实验室 | `lab-core.js`（822 行） | **近期所有开发都在这里，也是用户反馈的焦点** |

**技术栈**：原生 HTML/CSS/JS + Canvas 2D，无框架、无构建、无 npm 依赖。`index.html`（2081 行，含全部 CSS）+ 3 个 JS 文件。

---

## 2. 用户真正想要什么（最重要的一章）

用户对"动画实验室"的需求是通过 **7 轮真实反馈** 逐步收敛出来的。每一轮反馈都推翻了我的一部分实现。**理解这个演化过程，比看代码更重要**：

### 反馈演化史（按时间顺序）

1. **初始需求（v0.6）**：
   > "库存消失/卖完/超卖这些 forecast 理论我都需要可视化动画展示，我可以点击一键模拟，可以左右拖动参数来学习。**你还要教我怎么拖动，而不是直接让我自由——要引导我从不同的逻辑去下手拖动**"

   → 核心词：**可视化动画 + 一键模拟 + 可拖参数 + 引导式（非自由探索）**

2. **v0.6 后**："你这个动画太抽象了，一点都不基础，**你应该一个一个元素加进来，一个一个因子加进来**，而不是全部一起"

   → 诞生「搭积木式」：7 关递进，每关只加一个图形元素、只给相关滑块。

3. **v0.7 后**："为啥月均销量是一根直线？下面三角形的面积是啥？**这些完全没有介绍啊。要把数学知识、表怎么看从 0 教起，学一下 MATLAB 的教学方式**，一个一个概念引入，说清楚为啥用这个元素、为啥用直线/竖线、为啥会移动、代表什么、面积是什么"

   → 诞生「看图说话」：每关文案三段式（是什么→为啥这样画→业务含义）+ 看图词典。另外要求 **时间轴用中文"第 x 天"，不要 D0/D60**。

4. **v0.8 后**："最基本的逻辑，**我调整库存的时候，线是不是得上下移动**？这个都没有。而且点一键模拟后**一定要有动画演示——你预设一个条件变化，让我看看是怎么行动和变化的**，标准的模板的学习效果"

   → 修 y 轴自适应导致"线不动"的假象（第 1 关 y 轴固定 0~12000）；一键模拟改成 autoDemo（参数自动变到 demoTarget → 播放头扫描 → 恢复 baseline）。

5. **v0.8.1 后**："图的右边布局好乱，没有结构化，**也没有说图中的每个元素究竟是干嘛的，你的说明完全无关**。你能不能先审视一下我的要求，做个计划先"

   → 用户首次要求**先出计划再执行**。排查发现真 bug：文案在讲一条**代码里根本没画**的安全库存线。诞生 v0.9：侧栏卡片化 + **动态图例**（只列画布上真实存在的元素，与 draw() 渲染条件一一对应）。

6. **v0.9.1 后**："**把模拟场（图表本体）固定住，右边的才可以上下移动，大小也要大一点**"

   → 左侧 stage 整块 sticky 吸顶占满 100vh，画布 flex:1 撑满；右侧面板独立滚动。

7. **v0.9.2 后**："不同的日销，**斜线没有变，只是面积变了，不会奇怪吗**？"（附两张截图对比 demand=36 vs 52）

   → 根因是 x 轴固定 180 天导致右半边全是 0 值水平线。改成动态 xHorizon（斜线永远占 ~80% 屏宽）+ 斜线中点直接标注"斜率 = 每天少 N 件"和三角形面积公式。

### 从演化史提炼的「用户教学观」（后续开发必须遵守）

- **MATLAB/可汗学院式教学**：一次只引入一个概念，前面的概念是后面的地基。
- **每个视觉元素必须被解释**：图上画了什么，侧栏就必须讲什么；讲了什么，图上就必须有什么。**文案与画布不许有半点脱节**（这是用户说"说明完全无关"的雷区）。
- **拖动必须有明显的视觉反馈**：拖滑块时对应元素要"肉眼可见地动"。如果因为坐标轴自适应等原因导致"看起来没动"，就是 bug。
- **演示优先于自由探索**：每关先"看演示"（标准动画模板），再让用户自己拖。
- **数学从 0 教起**：坐标系、斜率、面积、竖线、虚线，都要解释"为啥用这个几何形状"。
- **中文语境**：时间轴、读数一律"第 N 天 / 今天 / 60 天后"，不要 D0/D-notation。

---

## 3. 动画实验室现状（v0.9.3）

### 3.1 七关结构（STAGES 数组，lab-core.js）

| 关 | 新增元素 | 可用滑块 | 教学点 |
|---|---|---|---|
| 1 | 📦 库存横线（+坐标系） | 期初库存 | 横=时间 竖=件数；线高=有几件货；y 轴固定 0~12000 让线明显上下动 |
| 2 | 📉 下坡斜线+三角形填充 | +真实日销 | 斜率=-日销；三角形面积=总卖出量；动态 xHorizon 让陡度可见 |
| 3 | 🔴 售罄日红竖线 | 同上 | 竖线精确标"哪一天卖完"；覆盖天数=库存/日销 |
| 4 | 🟣 预测虚线 | +预测日销 | 虚线=计划/没发生；两线重合=预测准 |
| 5 | 🟢 补货绿箭头+红缺货区 | +提前期+补货量 | 箭头=到货日；红区=售罄到到货之间零货可卖 |
| 6 | 🟠 安全库存琥珀虚线 | +安全天数 | 警戒线高度=安全天数×日销；不缩短物流、只把下单提前 |
| 7 | 🟡 促销尖峰 | +促销系数+促销开始日（8 滑块全开） | 促销=斜率临时放大；光打广告不补货=加速断货 |

每关配置项：`title / intro / instruct / observe / baseline / demoTarget / dict[] / newLabel / element`。

### 3.2 关键机制

- **动态图例 `LEGEND[12条] + renderLegend()`**：每条有 `when(p, m, si)` 谓词，与 draw() 的渲染条件**一一对应**——画布上有的元素图例才列出，拖参数导致元素消失时图例条目同步消失。每条三段式：👁 图里啥样 / 🤔 为啥这样画 / 💼 意味着。**改 draw() 时必须同步改 LEGEND 的 when 条件，否则重蹈"说明无关"覆辙。**
- **autoDemo()**：一键模拟三步曲——①滑块动画滑到 demoTarget → ②播放头 2.4s 扫过全时间轴 → ③自动恢复 baseline。每关"看演示"（demoStage）同理但更短。
- **动态 xHorizon**（v0.9.3）：`naturalEnd = ceil(inventory/demand)+5`，clamp(30,180)——保证斜线段永远占 ~80% 屏宽；网格刻度按 xHorizon 等分 5 段。
- **布局**：`.lab-stage` sticky 吸顶占满 `calc(100vh - 24px)`，画布 flex:1 撑满；`.lab-side` 独立 `overflow-y: auto`。
- **侧栏卡片顺序**：进度条 → 📚 本关学啥（含"本关图上新增"徽章）→ 🧩 图上现在有什么（动态图例）→ 🎮 怎么拖 + 👀 看什么变化（滑块在此）→ 📖 几何词典（累计式）。

### 3.3 测试

- 烟雾测试：`C:\Users\23805\WorkBuddy\2026-07-24-00-53-48\.workbuddy\research\verify_lab.js`（jsdom，**64 项断言**，当前全过）。
- 运行：`node verify_lab.js`（jsdom 已装在该 research 目录）。
- 要点：需手动派发 `DOMContentLoaded`；覆盖 `window.requestAnimationFrame = cb => { cb(1e12); return 1 }` 让动画同步完成；**不要**给 `global.performance` 赋值（会死循环）。
- **每次改 lab-core.js / index.html 的 lab 部分后必须跑一遍**；新行为要加断言。

---

## 4. 开发工作流（用户明确要求，不许改）

1. **本地模式，不部署**：用户原话"改完我马上看效果"。后台跑 `python -m http.server 4173 --bind 127.0.0.1`（在 `D:\Desktop\sku-simulator-mvp`）。改完文件→让用户 **F5 刷新** 即可。**不要调 CloudStudio 部署、不要 GitHub Pages。**
   - 桌面快捷方式：`D:\Desktop\SKU经营训练模拟器.url` → `http://127.0.0.1:4173/index.html`
   - 服务挂了（ERR_EMPTY_RESPONSE/ERR_CONNECTION_REFUSED）：杀掉旧 python 进程重启，或让用户双击 `D:\Desktop\启动SKU模拟器本地服务.bat`
2. **每个版本**：改代码 → 跑 verify_lab.js 全过 → 更新 README.md + DEVELOPMENT_LOG.md 版本号与版本记录 → `git add -A && git commit` → `GIT_TERMINAL_PROMPT=0 git push origin master:main`（凭据已存 `~/.git-credentials`，可直接推，**本地分支叫 master，远端叫 main**）。
3. **自己先验收再交付**：用 playwright-cli 打开 `http://127.0.0.1:4173`，resize 到 1280×720（贴近用户屏幕），切到动画实验室，截图确认视觉效果，再告诉用户"刷新看看"。**不要没看过效果就说改好了。**
4. **回复风格**：中文、通俗、讲"改了什么+你能感知到什么变化"，技术细节放次要位置。用户非开发背景。

---

## 5. 已知问题 / 待办

### 明确待办（用户提过）
- [ ] **Module D**：导入 Govee 真实 xlsx 数据（教学场景已基于 Govee 资料真实化，但还没做真实数据导入）
- [ ] **Module E**：周报/数据条工具网页化

### 潜在改进点（下一任 AI 建议审视）
- [ ] 第 3~7 关还没有像第 2 关那样的**图上直接标注**（v0.9.3 只给第 2 关加了"斜率=每天少N件"标签）。按用户教学观，后面几关大概率也会要求"图上直接讲"。
- [ ] 促销关（第 7 关）教学密度偏高（2 个新滑块一起进），可能被要求再拆。
- [ ] 手机端（<900px）lab 布局未专门适配（sticky 吸顶在窄屏可能挤压侧栏）。
- [ ] `sku-simulator-mvp.html`（83KB）是老的单文件版本，已废弃但没删——别改它，改的是 `index.html`。

---

## 6. 踩过的坑（血泪，务必读）

1. **y/x 轴自适应会杀死"变化感"**：坐标轴范围跟着参数走 → 拖参数时元素相对位置不变 → 用户觉得"没动"。**教学场景下，被拖动的维度必须固定坐标范围（或反向动态化让变化更明显）**。第 1 关 y 轴固定、第 2 关 x 轴动态化，都是这个原理的应用。
2. **文案与渲染脱节是最大雷区**：v0.9 前安全库存线文案写了三关，代码里根本没画。用户的原话是"你的说明完全无关"。动态图例的 when 谓词就是防线——**改渲染必须同步改图例**。
3. **baseline 参数必须让本关新元素默认可见**：demand=8 时三角形不闭合、promo=1 时尖峰不出现——用户一进关看不到要学的元素。每关 baseline 都调过，改时注意。
4. **Edit 工具对 index.html 多次替换失败**：该文件历史上有过 CRLF/编码问题，Edit 匹配不上时先 `Read` 精确段落、用更小的锚点字符串，或用 python 读字节确认实际内容。
5. **jsdom 测试三坑**：见 3.3 节（DOMContentLoaded / rAF / performance）。
6. **切关后的空引用**：buildSliders 会清空重建滑块 DOM，`state.inputs` 里的旧引用会失效——syncSliders/demoStage 都加了 guard，新代码访问 `state.inputs[k]` 前必须判空。
7. **本地 python http.server 会僵死**：端口 LISTENING 但不响应（跑数小时后）。症状 ERR_EMPTY_RESPONSE。处理：kill 重启。

---

## 7. 文件地图

```
D:\Desktop\sku-simulator-mvp\
├── index.html          # 2081 行。全部 HTML + CSS（lab CSS 搜 ".lab-"）
├── lab-core.js         # 822 行。动画实验室全部逻辑 ★ 主战场
│   ├── PARAMS          # 8 个参数定义（min/max/step/unit）
│   ├── STAGES[7]       # 7 关配置
│   ├── LEGEND[12]      # 动态图例（when 谓词）
│   ├── computeSeries() # 库存序列/售罄日/缺货天数/到货日
│   ├── draw()          # Canvas 渲染（元素按关解锁）
│   ├── autoDemo()/demoStage()  # 一键模拟/看演示动画
│   └── applyStage()/renderLegend()/updateReadout()
├── teach-core.js       # 632 行。教学训练+沙盘（Tab 切换在这，3 态）
├── teach-data.js       # 教学题库（基于 Govee 真实资料）
├── README.md           # 含完整版本记录 v0.1→v0.9.3
├── DEVELOPMENT_LOG.md  # 开发日志
├── sku-simulator-mvp.html  # ⚠️ 废弃的老单文件版，别碰
└── SKU经营训练模拟器-开发交接文档.md  # v0.1 时代的旧交接文档，已过时

测试：C:\Users\23805\WorkBuddy\2026-07-24-00-53-48\.workbuddy\research\verify_lab.js
GitHub：远端 main 分支（本地 master），推送凭据已配好
```

---

## 8. 给下一任 AI 的审视清单

接手时建议按此顺序验证：
1. 打开 `http://127.0.0.1:4173/index.html`（服务没跑就在项目目录起 `python -m http.server 4173 --bind 127.0.0.1`）
2. 走完 7 关：每关①先看"图上现在有什么"是否与画布一致 ②点"看演示"确认动画完整 ③拖每个滑块确认视觉反馈明显
3. 跑 `node verify_lab.js` 确认 64/64
4. 对照第 2 章"用户教学观"，找出当前版本仍违背的地方（比如第 3~7 关缺图上标注）——那大概率就是用户下一轮反馈的内容，可以主动改
