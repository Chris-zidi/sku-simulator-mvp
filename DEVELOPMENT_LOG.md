# 开发记录

本文件用于让不同电脑、开发者和开发 Agent 在不依赖聊天上下文的情况下继续开发。

每次完成一个可提交改动时，都应在文件顶部追加一条记录，并包含：

- 日期与版本；
- 改动内容；
- 验证方式与结果；
- Git 提交；
- 遗留问题或下一步。

## 当前开发规则

1. 开发前执行 `git pull --rebase`。
2. 完成改动后先验证，再提交和推送。
3. 默认同步到 GitHub；只有项目负责人明确要求时才保留为本地修改。
4. `main` 必须保持可运行。
5. 高风险功能或重构使用独立分支。
6. 不强制推送，不覆盖其他设备或开发者的提交。
7. 不提交真实公司敏感数据、账号凭据、密钥或临时文件。

## 版本记录

### v0.17.0 - 2026-07-27

状态：① 观察步骤加逐项对错解析 + 复盘展示。用户看着"① 观察"截图问："像这一步的选择，有没有准确的答案或者每个选项应该选是不是有解析的？是需要解析的哦，不然的话我们学习的目的是要追求正确的……这样子整个关卡过起来才有收获。"

**诊断**：`teach-data.js` 里每个 `observeOptions` 早就有 `exp`（该不该勾选，true/false）字段——`scoreChapter()`（`teach-core.js:37`）已经在用它算"数据观察"这个分数维度（`hits`/`misses` 对比 `st.obs`），但**从来没有把哪些该勾、为什么、用户实际选没选对，展示给用户看过**，只在最后给一个 0-100 的抽象分数。这正是用户说"没有收获"的原因——分数没法告诉你具体错在哪。

**顺手挖到一个真 bug**：第 7 章 `observeOptions` 里有一处 `{ k: "缺口 = 3360 − 2200 = 1160", ok: true }`——跟其他所有选项用的 `exp` 字段名不一致，是个拼写错误。`scoreChapter()` 读取的是 `.exp`，这个选项的 `exp` 因此是 `undefined`（falsy）：① 不计入 `expIdx`（该勾选项总数分母算少了）；② 如果用户勾了这个本该勾的正确选项，会被 `misses = st.obs.filter(i => !loop.observeOptions[i].exp).length` 误判成"误选"倒扣分——选对了反而扣分，一个安静了很久的真 bug。

**改动**：

1. **数据**（[teach-data.js](D:\Desktop\sku-simulator-mvp\teach-data.js)）：给 11 章全部约 60 个 `observeOptions` 补上 `why` 解析字段——不是凭空编，是结合每章已有的 `truth.note`/`ruleHint`/`concept` 归纳出的简短原因（该勾的说清楚"为什么这是判断的关键信号"，不该勾的说清楚"为什么这是干扰项/编造的信息"）。顺手把 c7 那处 `ok: true` 改成 `exp: true`，修好这个隐藏 bug。

2. **渲染**（[teach-core.js](D:\Desktop\sku-simulator-mvp\teach-core.js) `renderChapter()` 的"④ 看结果"分支）：新增逐项复盘列表，对每个观察选项按用户"勾没勾"×数据"该不该勾"算出四态——命中✓（该勾且勾了）/漏选○（该勾但没勾）/误选✗（不该勾却勾了）/正确跳过（不该勾也没勾，低调显示）——每行配 icon + 该选项的 `why` 解析文字。配色全部用项目已有的深浅色主题变量（`--green`/`--amber`/`--red`/`--muted` 及对应的 `-soft` 底色），不引入硬编码颜色。`scoreChapter()` 本身的评分算法完全不动，只是把它一直在用但从没展示的信息摊开给用户看。

3. **测试**（`C:\Users\23805\WorkBuddy\2026-07-24-00-53-48\.workbuddy\research\verify_lab.js`）：新增数据完整性断言（11 章全部 `observeOptions` 的 `why` 非空、`exp` 都是布尔值——这条断言在修 bug 之前就能抓到 c7 那个 `ok` 拼写问题）；新增渲染回归断言——故意在 c1 混合勾选（该勾的勾一个、该勾的漏一个、不该勾的误选一个），确认渲染出的 `.obs-review-row` 数量、`hit`/`miss`/`wrong` classList、每行是否都带了非空解析文字全部对得上。`verify_lab.js` 从 99 项扩到 **106 项**，全过。

**验证**：
- `node verify_lab.js`：106 项断言全过（v0.17 新增的数据完整性 + 渲染回归断言在改代码之前跑必然失败，先确认能抓到问题再动手修）。
- 浏览器实测（`javascript_tool` 直接量）：模拟真实混合勾选场景（命中/漏选/误选/正确跳过各至少一例），确认渲染出的每一行 class 和解析文字都对得上预期；1920px 宽屏与 375px 手机宽度均零横向溢出；深色主题下四种状态图标颜色（`rgb(65,176,131)`/`rgb(224,162,76)`/`rgb(239,107,98)`/`rgb(154,161,168)`）精确匹配 `--green`/`--amber`/`--red`/`--muted` 的深色主题值；控制台无报错。

遗留：教学训练剩下的 9 章 `lesson` 增强内容仍未按 v0.16 样板重写；`sku-simulator-mvp.html` 废弃单文件版清理仍是遗留任务。

### v0.16.0 - 2026-07-27

状态：教学训练「先学后练」重构（2 章样板）。用户看着教学训练截图说"这一块还是不够好"，给了参考项目 `D:\Desktop\销售预测与备货学习页\运营周会能力工作台.html`，要求参考它重新设计教学活动体验。方向已用 AskUserQuestion 确认：先学后练（保留六步循环/经验本/评分/解锁机制）+ 先做 2 章样板。

**诊断（先于任何代码改动）**：`teach-data.js` 里 11 章每章都写了 `concept`（概念讲解）和 `analogy`（生活类比），内容质量不差，但 grep 整个 `teach-core.js`——**这两个字段一次都没被渲染过**，是彻底的死数据。`renderChapter()` 实际只渲染 `problem` 长段落 → 六步循环，真实体验是"一段 150~260 字的密集文字，直接让你猜数字"，中间没有"教"这一步——这才是"不够好"的直接来源。顺带对照参考项目逐条列出了它强的地方（有观点的标题、具体人物+商品+数字、公式单独拎出来、标准例题三段式、错误vs正确写法对照、明确标注"有固定答案"vs"没有唯一答案"），也明确了不照抄的两个坑：它的 `.metric` 类没定义 CSS（不生效）、颜色是硬编码十六进制（本项目有深浅色主题，必须用项目已有的 CSS 变量）。

**改动**：

1. **先学后练状态机**（[teach-core.js](D:\Desktop\sku-simulator-mvp\teach-core.js)）：`openChapter()` 把初始 `state.step` 从 `0` 改成 `-1`（先学讲解阶段）；`renderChapter()` 新增 `state.step === -1` 分支——只渲染讲解块 + 「我懂了，开始练 →」按钮，六步循环完全不出现；`bindChapterEvents()` 新增 `startPractice`（进入练习）/`backToLesson`（回看讲解）两个 action。练习阶段顶部加「↩ 回看讲解」链接，`state.draft` 不受 `step` 影响，来回切草稿不丢。经验本/四维评分/沙盘解锁三套机制完全不动。

2. **`renderLesson(ch)` 分两层**：兜底层渲染已有但一直没显示的 `ch.concept`/`ch.analogy`（11 章立即生效，9 个未做样板的章节也从"零教学"变成"有概念+有类比"）；增强层是新的可选 `lesson` 数据结构——`headline`（有观点的句子，不是术语名）/`scene`（具体人物+商品+数字）/`cards`（并列对比卡，`grid-template-columns:repeat(auto-fit,minmax(220px,1fr))` 自动换行不用写视口断点）/`worked`（标准例题：题面→分步推导→固定答案）/`contrast`（错误写法 vs 正确写法对照）/`say`（收敛回实际工作）。字段全部可选，缺什么跳过什么。

3. **2 章样板数据**（[teach-data.js](D:\Desktop\sku-simulator-mvp\teach-data.js)）：c1「Forecast 到底是什么」（新人阿哲接手 Govee 智能感应灯，用同一批库存报不同 Forecast 数字对比供应链读出的"安全感"）、c5「库存覆盖+加急提拉物流」（运营晓雯接手车载香薰，标准例题算出"最晚下单日=负数"这个明确的行动信号）。两章都在先学阶段标注"标准例题 · 有固定答案"，跟练习阶段"自由判断 · 没有唯一答案"明确区分开——这正是用户在动画实验室那几轮反复强调的诉求，也是参考项目做得好的地方。

4. **CSS**：新增 `.ls-*` 系列样式类，全部用项目已有主题变量（`--accent`/`--green`/`--red`/`--panel`/`--paper`/`--line`/`--accent-soft` 等），不引入硬编码颜色；对比卡/错误正确对照都用 `auto-fit`/`minmax` 自适应换行，比写视口媒体断点更彻底地满足"零横向滚动"这条项目死规矩。

5. **测试基建修复**（`C:\Users\23805\WorkBuddy\2026-07-24-00-53-48\.workbuddy\research\verify_lab.js`）：发现测试脚本**从没读取过 `teach-data.js`**——`teach-core.js` 的 `init()` 在 `DOMContentLoaded` 时会调 `switchTab("teach")` → `renderHome()`，引用全局 `CHAPTERS`，而 `CHAPTERS` 是 `teach-data.js` 定义的，没加载它，一跑就抛 `ReferenceError: CHAPTERS is not defined`（被 VirtualConsole 吞掉）——**教学训练引擎在测试里从来没有真正跑起来过**，跟 v0.12 那次"draw() 从没被真正执行过"是同一类问题。修的过程中还发现一个 jsdom 真问题：`window.eval()` **每次调用会创建独立的顶层词法作用域**，`const`/`let` 声明不跨多次 `eval()` 调用共享（跟真实浏览器里多个 `<script>` 标签共享同一个全局词法环境的语义不一样）——单独 `eval` 三个文件必然复现 `CHAPTERS is not defined`，改成拼接成一个字符串一次性 `eval` 解决。新增断言：`CHAPTERS` 结构检查（11 章、必修8/选学3、每章 concept/analogy 非空）、先学后练核心回归（打开章节先看不到"① 观察"，点「开始练」后才出现）、样板渲染（c1/c5 有 `lesson.worked.answer`，先学阶段能看到"标准例题"标注）、死数据回归（用永远解锁的选学章节 a1 验证兜底层 concept 确实被渲染出来，没做样板的章节也受益）。`verify_lab.js` 从 84 项扩到 **99 项**，全过。

**验证**：
- `node verify_lab.js`：99 项断言全过（v0.16 新增的 8 项在改代码之前跑必然失败，先确认能抓到问题再动手修）。
- 浏览器实测（`javascript_tool` 直接量真实数字，不靠肉眼）：c1 先学阶段确认看到 headline/场景/对比卡/标准例题、看不到六步循环；点「开始练」后循环正确出现；来回切「回看讲解」草稿不丢；完整走完 c1~c4 解锁 c5，确认 c5 样板内容正确渲染；c2（未做样板）确认兜底层 concept/analogy 正确显示；1920px/375px 两档宽度零横向溢出，卡片正确换行/降列；深色主题下卡片背景/公式文字/边框颜色全部正确跟随主题变量（不是硬编码）；经验本（localStorage journal）、四维评分、章节解锁三套机制全程行为与改动前一致。

遗留：剩下 9 章的 `lesson` 内容尚未按样板重写（本轮通过兜底层已有 concept/analogy 显示，用户确认样板口味满意后再推广）；`sku-simulator-mvp.html` 废弃单文件版清理仍是遗留任务。

### v0.15.0 - 2026-07-27

状态：修压仓标签越界 + 干掉卡顿 + 用现代浏览器能力根除两类反复复发的 bug。用户三连反馈："UI 还是被挡住了，还出现了乱码""现在是不是用了最新最好的技术栈呢""改完之后也变卡了，有些按钮点了半天都没有反应"。这次先进 Plan Mode 做了完整只读诊断，跟用户确认方向后再动手——用户选的是"保持零构建，升级到现代浏览器能力"，不换 React/Vue。

**诊断结论（先于任何代码改动）**：
- "乱码"不是编码问题（文件是干净 UTF-8）——是 `🏚 压仓 8 件` 标签叠在 `180 天后` 上面。
- "变卡"是真的：`cssVar()` 每帧调 20+ 次 `getComputedStyle()`（强制同步重算样式）、`computeSeries()` 每帧算两遍（`draw()` 一遍 `updateReadout()` 一遍）、热路径两处 `innerHTML` 全量重建——「一键模拟」145 帧全跑这套，主线程被占满。
- 技术栈：React/Vue 不会自动修好这三类问题（都是自己代码的问题），但 ResizeObserver、CSS 容器查询这两个原生能力确实该用而没用。

**改动**：

1. **修压仓/缺货标签越界**（[lab-core.js](D:\Desktop\sku-simulator-mvp\lab-core.js) `clampLabelY()`/`draw()`）：底部安全线从写死的 `H-24` 改成统一算出的 `labelMaxY = H - padB - 6`（比 x 轴日期文字再高 6px 的缓冲）；压仓标签块高 <20px（退化成一条缝）时不再标 `fixed:true`，参与 `placeLabels()` 避让。根因是旧边界离日期文字只差 14px，`endInv` 很小时压仓块贴 0 线，标签被夹到边界正好压字。

2. **渲染管线重构**（[lab-core.js](D:\Desktop\sku-simulator-mvp\lab-core.js)）：
   - `cssVar()` 加 `Map` 缓存，`init()` 里用 `MutationObserver` 监听 `data-theme` 属性变化时清空缓存——切主题的联动链路用真实浏览器验证过（`draw()` 实际用色跟着新主题变，不是缓存里的旧值）。
   - 新增 `requestRender()`/`renderNow()` 单一渲染入口：`requestRender()` 用 `state.renderQueued` 标志把同一帧内的多次触发去重到一次 `requestAnimationFrame` 回调；`renderNow()` 里 `computeSeries()` 只算一次，结果分给 `draw()`/`updateReadout()`/`renderNodeCard()` 三个消费者。`setDay()`、`setParam()` 的三个分支（choice/动画 tween/普通赋值）全部从直接调 `draw()+updateReadout()` 改成调 `requestRender()`——`demoStage()` 对两个 `demoTarget` key 各起一条 tween 循环时，同一帧最终只触发一次渲染，不再是每帧 N 份 `innerHTML` 重建。
   - `renderNodeCard()`/`updateReadout()` 从"每帧整段重建 `innerHTML`"改成"骨架建一次（复用已有的 `state.lastLegendKey` 跳过重建的套路，新增 `state.lastReadoutKey`/`state.readoutCells`/`state.nodeCardEls`），行/结构集合没变时只更新文字"。
   - `unlockedElements()`/`stageIndexOf()` 按 `stageIndex`/`name` 记忆化，不再每次调用都新建 `Set`/全表扫描（`hasElement()` 到处在调，原来是热路径里隐藏的分配开销）。
   - 浏览器实测：促销关连续 100 帧 `renderNow()` 的 p95 帧耗时 5.3ms（60fps 预算 16.7ms 内，留了 3 倍多余量）；「一键模拟」2400ms 扫描期间用 `PerformanceObserver` 监听 `longtask` 实测**零长任务**（改前主线程被占满导致"点了半天没反应"）。

3. **ResizeObserver 根除画布尺寸不同步这一整类 bug**（[lab-core.js](D:\Desktop\sku-simulator-mvp\lab-core.js) `init()`）：`new ResizeObserver(() => { resizeCanvas(); requestRender(); }).observe(state.canvas)`——画布 CSS 尺寸一变就自动同步像素缓冲区，不用再满世界找"这个操作会不会改变画布可用空间"的调用点手动补 `resizeCanvas()`（v0.14.4 就是这么一个个补出来的，属于治标）。这次不会重蹈 v0.11 那次 ResizeObserver 翻车的覆辙——那次是"JS 测顶栏高度→写 CSS 变量→改变布局→又触发测量"的反馈环；这里 canvas 的 CSS 尺寸完全由 CSS 决定，`resizeCanvas()` 只改像素缓冲区，不影响任何布局，没有回环。关键路径（`applyStage()`/`restoreSnapshot()`，即切关卡）保留了显式同步调用——按浏览器规范 ResizeObserver 通知在同一帧的 rAF 之后才送达，只靠 observer 会有一帧用旧尺寸画的轻微闪烁，显式调用保证零延迟。诚实说明：ResizeObserver 的实时触发没能在这次用的自动化浏览器面板里直接观测到——面板处于未合成渲染帧的状态（跟 `computer{screenshot}` 一直失败是同一个环境限制），ResizeObserver 的回调恰好挂在渲染管线上，这个环境天然测不到。但用户可见的原始 bug（切关卡后画布变形）已经用不依赖 ResizeObserver 时机的显式同步调用验证修复（连续切关 3 轮 × 7 关，`canvas.width/height` 与 `clientWidth×dpr` 全部一致）。

4. **CSS 容器查询让面板按自身宽度适配**（[index.html](D:\Desktop\sku-simulator-mvp\index.html)）：`.lab-controls`/`.lab-stage`/`.lab-lesson` 都声明成 `container-type: inline-size`，choice 按钮组（`@container ctrls (max-width: 250px)`）、读数面板列数（`@container stage (max-width: 560px/360px)`）改用容器查询触发，取代原来挂在 `@media (max-width: 1100px)` 视口断点上的规则——面板实际宽度是三栏 grid 分配决定的，跟视口宽度从来就不是一回事，这是 v0.13→v0.14.4 反复调三栏比例、每次都要重新试断点的根本解法。顺手确认了一个此前没被注意到的细节：`.lab-demo-badge` 的定位基准其实是 `.lab-canvas-box`（HTML 里有内联 `style="position:relative"`），不是 `.lab-stage`，所以容器查询给 `.lab-stage` 加的 containment 不会像最初担心的那样影响演示徽章的位置——仍然给 `.lab-stage` 补了显式 `position: relative`，把意图钉死，不依赖任何隐式副作用。

5. **测试加固**（`C:\Users\23805\WorkBuddy\2026-07-24-00-53-48\.workbuddy\research\verify_lab.js`）：重叠检测从固定 `Math.abs(A.y-B.y)<13` 改成按每条标签的真实字号（从 `ctx.font` 解析）算包围盒——这正是本轮压仓标签 bug（实际压字 14px）能从旧阈值指缝里漏过去的原因。调这套新算法时又抓到一个边界案例：日销关两条教学标签故意留了 14px 间距、从未真正压过字，第一版的字号系数（0.85 ascent + 0.3 descent）把它误判成重叠——把系数调到更贴近真实无衬线字体墨迹范围的 0.75/0.2 后不再误报，同时仍然抓得住真正的越界。新增退化场景断言（把 `forecast` 调到让 `endInv` 落在 1~20 件，锁定本轮 Part 1 的修复）、性能锁定断言（`renderNow()` 只应触发 1 次 `computeSeries()`、`getComputedStyle` 调用数 <10）、`ResizeObserver` no-op stub（jsdom 没有这个 API，不 stub 的话 `init()` 里那段代码在测试环境完全跑不到）。`verify_lab.js` 从 80 项扩到 **84 项**，全过。

**验证**：
- `node verify_lab.js`：84 项断言全过（Part 1/5 的新断言在改代码之前跑是失败的，先确认能抓到问题再动手修）。
- 浏览器实测覆盖 1920×1000（宽屏桌面）、1290×700（复现用户截图的窄高比）、1150×900（三栏断点边缘）、1050×900（刚好堆叠）、375×800（手机）五档尺寸：性能（p95 帧耗时、零长任务）、压仓标签精确复现原始 `endInv=8` 场景确认不再越界、容器查询按自身宽度切换列数（choice 按钮组、读数面板）、demo-badge 定位、主题切换的 cssVar 缓存失效链路，全部通过。

遗留：教学训练 Tab「先学后练」重构仍未开始；`sku-simulator-mvp.html` 废弃单文件版仍未清理；ResizeObserver 的实时触发行为建议下次有真实设备/非自动化浏览器环境时人工复核一次（当前只做了结构性验证：无报错、关键路径不依赖它）。

### v0.14.4 - 2026-07-27

状态：修画布变形真 bug + 文案栏太窄 + 矮屏幕画布/读数抢地盘。v0.14.3 推送不久，用户反馈"连续几次切换之后，这个文字就会变形呢？然后重新缩放了一下就又不会了"，又发了张预测关截图指出"下面的文字被截断了，UI也要注意一下智能适应啊"。三个问题分开定位、分开修。

**问题一：连续切关卡后画布文字"变形"，缩放窗口又恢复正常**

这是真 bug，不是错觉。根因是 Canvas 有两套独立的尺寸：CSS 渲染尺寸（`clientWidth`/`clientHeight`，决定它在页面上占多大）和像素缓冲区尺寸（`canvas.width`/`canvas.height`，决定内部实际画了多少像素）。`resizeCanvas()` 负责把两者同步（`c.width = clientWidth × dpr`），但代码里只在切 Tab（`onShow()`）和 `window resize` 事件里调用——切关卡本身完全不触发它。

更深一层：`setDay()` 内部执行顺序是先 `draw()`（用旧的画布尺寸画）后 `updateReadout()`（重建读数面板 HTML，行数随关卡变化，比如"参考建议日"这行只在安全关起才出现）。读数面板是画布的 flex 兄弟节点，行数一变，画布能分到的高度也跟着变——但这个变化发生在 `draw()` 已经画完之后，没人再校准一次。第一次尝试的修法（在 `applyStage()` 里加一行 `resizeCanvas()`）也没修对，因为加的位置还是在 `updateReadout()` 之前，量到的是过渡态尺寸——连续切了 3 轮 × 7 关用脚本核对 `canvas.width`/`canvas.height` 是否等于 `clientWidth × dpr`，21 次检查里大部分不一致，而且呈现"总是差一步"的规律（这一次量到的是上一次 `updateReadout()` 之前的尺寸）。

真正的修法：把 `applyStage()`/`restoreSnapshot()` 里的调用顺序改成"先让 `updateReadout()` 把内容落定 → 再 `resizeCanvas()` 量准最终尺寸 → 最后 `setDay()` 正式画"。`updateReadout()` 会被多调一次（`setDay()` 内部还会再调一次），但这个函数是幂等的（纯粹从 `state.current` 重新渲染，不依赖调用次数），没有副作用。改完后 21 次检查全部一致。这个 bug 解释了用户描述的两个现象：连续切关卡触发（每切一次错位积累一次）、缩放窗口能修好（`resize` 监听里正确调用了 `resizeCanvas()`，重新校准一次）。

**问题二：文案栏文字看起来"被截断"**

排查后不是真的截断（`scrollWidth === clientWidth`，没有内容被裁切），是 v0.14.3 把图表份额提得太高（1.9fr），文案栏被压缩到 292px（1290px 笔记本宽度下实测），中文长句每行只塞得下 12~14 个字，短促的换行视觉上很像截断。改法：文案栏地板从 260px 提到 320px，份额从 0.85fr 提到 0.95fr，图表让出一点（1.9fr→1.75fr）。图表依然是主角（1920px 宽屏下还是有 850px+），但不能为了大而把唯一负责"读长句子"的那栏压到不能用。

**问题三（排查问题一过程中顺带发现的独立 bug）：矮屏幕下画布被挤到只剩 170px，y 轴刻度互相压字**

桌面三栏模式下 `.lab-canvas-box{flex:1; min-height:0}` 原来没有最小高度地板，纯粹是"分剩下的空间"。1366×768（当下最常见的笔记本分辨率之一）+ 促销关（读数面板 12 行，`.ro-grid` 3 列排布要占 4 行高度）的组合下，画布被压到 170~280px 之间，y 轴 5 档刻度标签在这个高度下互相压字——这跟布局宽度无关，是纵向版的"核心可视区被固定预算挤没"，跟 v0.14.3 修的移动端画布归零是同一类问题，只是这次发生在桌面模式。

改法：`.lab-canvas-box` 补 `min-height:280px` 硬地板；`.lab-readout` 从 `flex-shrink:0`（永不收缩，超出的部分被 `.lab-stage` 的 `overflow:hidden` 默默裁掉，用户根本看不出来少了什么）改成 `flex-shrink:1; overflow-y:auto`（矮屏幕下画布先顶住地板，读数面板让出空间被压缩，但压缩到装不下时自己出现一条小滚动条，而不是无声消失）。1366×768 下逐关实测：`.lab-stage` 自身的裁切量从最多 41px 降到 0，读数面板改成自己内部小滚动（7~41px 可滚动），画布稳定在 280px 以上。

**验证**：
- 浏览器实测覆盖 1920×1000（宽屏桌面）、1366×768（最常见笔记本分辨率）、1290×700（用户截图复现的窄高比）、830×900（移动断点内）、375×800（手机）五档尺寸；每档都跑了 7 关的画布尺寸同步检查（问题一）、文案栏/参数栏零溢出检查（问题二）、`fillText` 拦截零重叠检查（问题三），全部通过。
- `verify_lab.js` 复跑 80 项断言全过（纯 CSS/调用顺序调整，不涉及计算逻辑）。

遗留：教学训练 Tab「先学后练」重构仍未开始；`sku-simulator-mvp.html` 废弃单文件版仍未清理。

### v0.14.3 - 2026-07-26

状态：图表居中 + 修移动端画布归零的真 bug。v0.14.2 推送后用户又发了促销关、预测关的截图，质疑"你确定吗？我在百分百的比例下看到都这样子"，并主动提出布局建议："或者把函数看板放中间会更好不？其他的东西放2边，给我们的核心留够足够多的空间"。

**先回应布局建议——同意，直接实施**：量了一下 1920px 宽屏下的实际渲染，图表舞台只有 **539px CSS 宽**（`#labView{max-width:1440px}` 封顶 + 图表列只分到 1.25/3.05 的份额），确实太局限。改成图表居中：
- `.lab-grid` 三列权重从 `1.25fr : 0.75fr : 1.05fr`（图表:参数:文案）改成参数/图表/文案 `0.62fr : 1.9fr : 0.85fr`，`#labView` 上限从 1440px 放宽到 1680px。
- 用 CSS `order` 属性做视觉重排（`.lab-controls{order:1}.lab-stage{order:2}.lab-lesson{order:3}`），不改 HTML 里三个 `<aside>`/`<div>` 的实际 DOM 顺序——`lab-core.js` 里没有任何代码依赖这三者的 DOM 先后关系（`buildSliders()`/`renderStageChrome()` 等全部用 `getElementById` 精确取元素），用 `order` 视觉重排是这里风险最低的做法。
- 效果：1920px 宽屏下图表 CSS 宽度 539px → **887px**；1150px（3 栏断点边缘）下也从原来偏窄涨到 594px。

**再查用户截图里的真实重叠**——这不是"没修干净"，是改布局时顺手发现的一个独立真 bug：预测关截图里"参考 第71天"和"下单 第85天"两个事件轨标签、"参考建议"和"你的预测（计划）"两条图内标签，肉眼看确实压在了一起。用 `fillText` 拦截在真实浏览器里逐关测量后定位到根因——跟布局宽度无关，是**移动端画布高度被挤没了**：
- 移动断点（≤1100px）原来给 `.lab-stage` 写死 `height:55vh`，画布区（`.lab-canvas-box`）只是 `flex:1` 分剩下的空间。
- v0.11/v0.14 陆续给读数面板加行（促销关读数现在有 **12 行**，移动端 2 列排布要占 300+px 高），chips 行也越到后面越多、更容易换行——固定的 55vh 预算里，读数+chips+播放条吃掉大半，画布被挤到 **0px 高度**（`.lab-canvas-box{min-height:0}` 允许它被压没）。
- 画布高度=0 时，Canvas 内部坐标系仍然照常计算（`draw()` 不会因为高度 0 就跳过绘制），所有原本该分布在几百像素高度上的标注全部挤压进一条缝——这才是用户截图里"文字叠在一起"的真正原因，不是标注避让算法失效。
- 改法：舞台整卡不再限死总高度（`height:auto`，撑多高算多高，反正移动端 `#labView{overflow:visible}` 本来就能整页滚动），只给画布本身一个 `min-height:320px` 的硬地板——读数要多高就多高，画布再也不用跟它抢同一块固定预算。

**验证**：
- 浏览器实测（真实 Canvas + `fillText` 拦截，不是靠肉眼数像素）：830px 视口（复现用户截图的窄窗口）下，7 关画布高度全部稳定在 390px（远高于 320 地板），零重叠；1920/1150 两档桌面宽度下画布分别是 887px / 594px CSS 宽，图表舞台/参数栏/文案栏的横向溢出量全部是 0，视觉顺序（参数→图表→文案）用 `getBoundingClientRect().left` 验证正确。
- `verify_lab.js` 复跑 80 项断言全过（纯 CSS 改动，不涉及计算逻辑）。

遗留：教学训练 Tab「先学后练」重构仍未开始；`sku-simulator-mvp.html` 废弃单文件版仍未清理。

### v0.14.2 - 2026-07-26

状态：修标签文字被拆行的问题。v0.14.1 刚推送，用户又发了预测关、安全关的截图："到最后2关还是出现了一样的情况，还是不够结实啊"——这次不是横向滚动条了，是标签文字本身被硬拆成两行："下单日"拆成"下单"+"日"，"安全库存参考"拆成"安全库存参"+"考"，"预测日均销量"拆成"预测日均"+"销量"。

**根因**：v0.14.1 把 `.lab-field` 和 `.lab-field-top label` 都加了 `min-width:0` 以修横向溢出，但说明文字（`.lab-note-inline`）当时还嵌在 `<label>` 内部，跟标签文字共用同一个 flex 行抢空间。加了 `min-width:0` 之后，栏宽不够时 flex 布局会让**标签本身的文字**去换行填补空间，而不是本该被压缩/省略的说明文字——本质上是把"该被裁剪的内容"和"不该被裁剪的内容"塞进了同一个可收缩容器里，容器一收缩，两者一起遭殃，而且收缩优先级还选错了对象。

**改法**（比 v0.14.1 更彻底，不是又一次"哪里窄了补哪里"的局部打补丁）：把说明文字从 `<label>` 内部搬出来，变成 `.lab-field-top` 下面单独一整行的块级元素，允许自然换行（不再用省略号截断，信息永远完整可见）。这样标签行只剩「标签文字 + 新徽章」两个很短的元素，无论栏宽多窄都不会被拆断——从根上把两种不同性质的内容（必须完整、不可拆的标签 vs 可以换行、可以变多行的说明）分离到两个独立的布局单元，而不是共享一个收缩预算。`buildSliders()` 里两处重复的标题行 HTML（choice 分支和普通滑块分支）顺带提成了一个 `headerHTML` 共享变量，减少以后改动时两处遗漏的风险。

**验证**：
- 浏览器实测：1920/1280/375 三档视口宽度下，7 关所有参数行的 `<label>` 高度全部是单行（~19-20px，用 `getBoundingClientRect().height` 量的，不是肉眼估），没有一个被拆行；说明文字全文可读，不再有省略号截断（之前 v0.14.1 的方案即便不溢出也会截断长说明）；`.lab-controls` 横向溢出量在所有测试宽度下都是 0。
- `verify_lab.js` 复跑 80 项断言全过（纯 HTML 结构调整，不涉及计算逻辑，`buildSliders()` 外部行为不变）。
- 已更新持久记忆 [[sku-simulator-ui-overflow-rule]]，补充"必须完整显示的内容（标签）和可截断/可换行的内容（说明文字）不能塞进同一个可收缩 flex 容器"这条教训。

遗留：教学训练 Tab「先学后练」重构仍未开始；`sku-simulator-mvp.html` 废弃单文件版仍未清理。

### v0.14.1 - 2026-07-26

状态：修参数控制栏横向溢出。v0.14.0 刚推送，用户就发截图反馈"为啥中间这块会挤在一起呢？不要这样啊，不需要这种左右移动，很干扰视野的""你还要适配一下浏览器缩放的比例""越到后面压的越扁"。

**根因诊断**（用 javascript_tool 在真实浏览器里量出来的，不是猜的）：
1. `.lab-params { display:grid; gap:6px }` 没写 `grid-template-columns`——隐式网格列按内容最大宽度定轨。v0.14 新增的 choice 按钮组（物流方式/安全库存参考）是这一关里内容最宽的一行，把整个 `.lab-params` 列撑到 373px 宽，而 `.lab-controls` 栏本身只有 313px——所有行（包括普通滑块）跟着被逼宽。
2. `.lab-field` 没设 `min-width:0`，作为 grid item 默认 `min-width:auto`（= 内容最小宽度），没法被压缩到容器宽度以下。
3. `.lab-controls` 设了 `overflow-y:auto` 但没显式设 `overflow-x`——CSS 规范规定这种情况下 `overflow-x` 会隐式算成 `auto`（不是 `visible`），于是横向溢出的 60px 内容变成了一条横向滚动条，而不是自然换行或裁切——这正是截图里那条碍眼的横条的来源。
4. `.lab-choice-group` 原来用 `display:flex; flex-wrap:wrap`，4 个按钮换行后如果只剩 1 个孤立在新行，`flex-grow:1` 会让它独占整行宽度（截图里"旺季爆仓·140天"那个异常宽的按钮）。
5. `.lab-grid` 三栏 `minmax()` 地板：图表栏是 `minmax(0, 1.25fr)`，地板=0——窄屏或浏览器缩放导致有效视口变窄时，参数栏（220px）和文案栏（260px）的地板加起来先占满空间，图表栏被挤到接近 0，画布会被压成一条缝。原有的单一断点在 999px，而三栏地板之和（900px 左右）离断点太近，缩放到 110%~125% 时会落在"三栏但地板已经顶到头"的尴尬过渡区间，各栏硬挤变形。

**改法**：
- `.lab-params` 显式 `grid-template-columns: minmax(0, 1fr)`；`.lab-field`/`.lab-field-top` 补 `min-width: 0`——这是修复的关键一行，让 grid 列真正能收缩到容器宽度，不再被内容撑大。
- `.lab-choice-group` 从 `flex-wrap` 改成 `display:grid; grid-template-columns:repeat(2, minmax(0,1fr))`——固定两列，四个按钮永远等宽换行，不会再出现某个按钮忽然占满整行的怪样子；按钮内文字加 `white-space:normal; word-break:break-word` 允许换行，不再依赖裁切。
- `.lab-controls`/`.lab-lesson` 显式加 `overflow-x: hidden`——横向溢出一律裁掉，不给浏览器机会自动生成横向滚动条。这条连同上面的 `min-width:0` 一起写进了代码注释里，作为以后加新参数类型时的硬性检查项。
- `.lab-grid` 三栏都补上 min 地板：`minmax(360px, 1.25fr) minmax(220px, 0.75fr) minmax(260px, 1.05fr)`——图表栏不会再被压没；断点从 `max-width:999px` 提到 `max-width:1100px`，缩放到常见的 110%/125%/150% 都会落在单列堆叠这一侧，不再卡中间。
- 顺带把 `.lab-field` padding（7px→6px）、`.lab-params` gap（6px→5px）收紧了一点，纵向更紧凑。

**验证**：
- 浏览器实测（`javascript_tool` 直接量 `scrollWidth - clientWidth`，比截图更可靠）：1920/1440/1150/1050/768 五档视口宽度下，`.lab-controls`/`.lab-lesson`/`body` 的横向溢出量全部是 **0**；促销关（参数最多，8 个滑块+按钮）逐行检查也全部 0 溢出；1100px 断点前后分别确认三栏布局与单列堆叠都正常切换（用 `getComputedStyle(grid).gridTemplateColumns` 直接验证）。
- 中途踩了一个测试坑：改完 CSS 第一次量还是溢出，一模一样的数字——是 `python -m http.server` 不发缓存头、浏览器却缓存了旧 `index.html`（内联 CSS 在 HTML 里，没有 `?v=` 保护），加 `?nocache=` 查询串强制绕过缓存后才量到新样式生效。
- `verify_lab.js` 复跑 80 项断言全过（纯 CSS 布局改动，不涉及任何计算逻辑）。

遗留：教学训练 Tab「先学后练」重构仍未开始；`sku-simulator-mvp.html` 废弃单文件版仍未清理。

### v0.14.0 - 2026-07-26

状态：下单日改成玩家直接决策，提前期改成物流方式选择。用户看着「安全库存」关截图问"我还可以觉得补货提前期的长度的吗？我能掌控游戏规则的吗？不应该吧，我应该能掌控的是我决定下单的那一天"，追问更明确："安全库存天数应该就几种模式吧？我什么时候决定下单才是影响的关键因素吧？"

**病根**：旧版 `simulate()` 里下单那天是公式自动算出来的——`cover<=safetyDays+leadTime` 一旦成立就自动触发下单，玩家能拖的是 `leadTime`（该是客观条件）和 `safetyDays`（缓冲天数），但唯一真正决定"早订压仓/晚订断货"后果的变量——哪天点下单——玩家完全碰不到，是黑盒公式替玩家做的决定。

**改法**（用 AskUserQuestion 跟用户确认三点方向）：
- **新增 `orderDay`（下单日）滑块**：玩家直接拖动选哪天下单，取代自动触发公式，直接决定早订压仓、晚订断货的后果。`simulate()` 删掉逐天判断循环，改成 `arrivalDay = orderDay + leadTime` 一次性算，`ordered` 标志直接等于 `hasLead`。
- **`安全库存天数` 从连续滑块改成 4 档参考建议线**（0/7/14/21 天）：新增 `computeSafetyRefDay()` 延续旧公式算出一个"建议下单日"，只做参考、不再驱动任何结果；画布上从横向"安全线"改成竖向「📍 参考建议」虚线；`buildEventItems()`/`computeEvents()`/`updateReadout()` 都加了「你比参考建议早/晚 N 天」的对比文案。
- **`补货提前期` 从连续滑块改成 4 个物流方式按钮**（空运 20 天 / 海运快船 45 天 / 海运慢船 90 天 / 旺季爆仓 140 天）：用户原话"选物流方式才是对的，因为这个就是我要决策的"——这是一次性场景选择，不是逐日拖动的杆。
- **PARAMS 新增 `type:"choice"` 分支**：`buildSliders()` 按类型分支渲染 `<input type=range>` 或按钮组（挂 `.value` 属性保持下游 `setParam()`/`syncSliders()` 读写接口一致）；`setParam()` 对 choice 类型跳过数值补间动画（离散选择没有"中间值"）；`demoStage()`/`autoDemo()` 的参数应用与还原都统一走 `setParam()`，不再手写 `input.value=` 赋值（顺带修了一个用 `PARAMS[k].min/max` clamp choice 类型会得到 `NaN` 的潜在 bug）。
- **STAGES 改写**：补货关新增 `orderDay` 决策 + 物流方式选择，baseline 故意把下单日设晚制造断货，demoTarget 把它提前来修复对比；安全关讲"参考线"概念，baseline/demoTarget 对比切换缓冲档位后参考线怎么移动；预测关、促销关的 `leadTime` baseline 换成对应预设档。

**验证**：
- `verify_lab.js` 扩到 **80 项断言**：choice 类型改用点击按钮（`setChoice()` 新 helper）而不是设 `.value` 触发 `input` 事件；补货关滑块数 4→5（新增 orderDay）；安全关图例断言从"安全线"改成"参考建议"；重写了预测关的核心因果链断言——v0.14 下预测不再影响下单时机（新增"解耦"断言：orderDay/arrivalDay 不随预测变化），但仍然通过 `suggestedQty` 影响少赚/压仓金额；新增核心回归断言：同一关内下单日早/晚分别让压仓金额/少赚金额明显变化；新增参考建议日随缓冲天数单调递减的断言；新增 choice 按钮点击后 `.active` class 与 `state.current` 同步的断言；事件轨数量因新增「参考」事件从 4 个变成 5 个。全过。
- 浏览器实测（`http://127.0.0.1:8899`）：物流方式按钮点击后高亮正确切换、`state.current.leadTime` 同步更新；下单日滑块拖动后读数「下单日/到货日」「缺货情况」实时联动（leadTime 90 天时缺货 84 天，下单日提前到第 10 天后缺货降到 24 天）；安全关切换缓冲档位后「参考建议日」数值与手算公式完全一致（缓冲 7 天→第 49 天/晚 7 天，缓冲 21 天→第 35 天/晚 21 天）；节点卡文案正确显示"比参考建议晚 21 天，缺货风险更高"；一键模拟/看演示按钮点击不报错；控制台无报错。

遗留：教学训练 Tab「先学后练」重构仍未开始；`sku-simulator-mvp.html` 废弃单文件版仍未清理。

### v0.13.0 - 2026-07-26

状态：动画实验室三栏布局。用户看着截图说"把教学文案面板给独立出来，放到右边变成第三块吧，给他多点空间，而且调参数也不需要这么宽，可以挤一挤放中间就好"。

**改动**（纯 `index.html` HTML/CSS，未改 `lab-core.js`/`teach-core.js`）：
- `.lab-grid` 从两列 `minmax(0,1.35fr) minmax(0,1fr)` 改成三列 `minmax(0,1.25fr) minmax(220px,0.75fr) minmax(260px,1.05fr)`。
- 原来的 `.lab-side`（一个 `<aside>`，内部用 flex 竖排塞下 `.lab-side-fixed` 固定区 + `.lab-scroll` 滚动区）拆成两个独立的 `<aside>`：
  - `.lab-controls`：标题 + 节点卡 + 滑块 + 撤销/上一步/看演示/下一步导航，宽度收窄到约 300px，自己 `overflow-y:auto`。
  - `.lab-lesson`：本关学啥 / 图上现在有什么（图例）/ 怎么拖&看什么变化 / 看图词典四张卡片，独立一整栏，不用再跟参数栏抢垂直空间，宽度比原来的侧栏更宽。
- 窄屏媒体查询（`max-width:999px`）同步更新：三栏改成上下堆叠（图表→参数→文案），各自给了 `max-height` 避免单栏无限撑高。

**验证**：
- 浏览器实测 1280×800 下三栏宽度 500px / 300px / 420px，四条边全部对齐、不溢出视口；平板宽度（768px）下正确堆叠成三段。
- 图表画布因为三栏挤压变窄（474px，原来约 682px）——重新跑了一遍 v0.12 才写的图内标注避让算法压力测试，7 关零重叠，确认避让算法本身跟画布宽度无关，不会因为变窄就重新压字。
- `verify_lab.js` 里唯一依赖旧 DOM 结构的断言（`.lab-side .lab-card` 数量）改成 `.lab-lesson .lab-card`，其余全部不用动——因为拆栏没改任何 `id`，只挪了容器；73 项断言全过。

遗留：教学训练 Tab「先学后练」重构仍未开始；`sku-simulator-mvp.html` 废弃单文件版仍未清理。

### v0.12.0 - 2026-07-26

状态：图表可读性修复。用户截图反馈"越到后面元素都挤在一起卡死了，是不是坐标系还是有局限呢？要不要换技术栈？"——先答技术栈问题：不用换，D3/ECharts 同样要自己写标签避让，换栈只会丢掉已测的 1100 行引擎和当时 64 项断言。真病因是量化出来的两个坑，都是 v0.10 埋的：

1. **y 轴量程过大**：v0.10 的 `yMax = max(12000, 库存+补货量)` 这个 12000 硬地板，在库存只有 3000~5000 时严重过头，`makeTicks` 又把它撑到 15000，曲线被压进画布底部 26%~33%，73% 是空白。
2. **17 处 `fillText` 全是写死坐标，零避让**：促销关实测 9 个标注，5 个挤在 20 像素的带子里，中间三分之一区域一个标注都没有。

**改动**：
- **y 轴贴合 + 冻结**：新增 `niceTop(dataMax)` 替换 `makeTicks`，在 `[1,2,2.5,5]×10^n` 里挑分 3~5 档、顶部最贴近数据的组合。新增 `freezeStageYAxis()`，在 `applyStage()`/`restoreSnapshot()` 里按当前参数算一次并存进 `state.stageYTicks`，`draw()` 只读不重算——拖任何滑块坐标轴都不变，完整保住 v0.10「被拖动的维度必须锁死坐标域」那条铁律。新增 `growStageYAxisIfNeeded()`：库存冲出冻结的顶时只涨不缩。第 1 关（坐标系）例外，固定 0~12000 不变（滑块本身是教学对象，量程要覆盖全程）。实测利用率：日销/售罄 32%→96%，补货/安全 26%→99%，预测/促销 33%/27%→100%。
- **顶部事件轨**：新增 `buildEventItems()`/`layoutEventRows()`，售罄/下单/到货/促销开始四类事件统一画在画布顶部一条轨道上（不再散落在曲线周围写死坐标），按 x 坐标贪心横向避让，放不下自动换行；下单↔到货之间画「提前期 N 天」括号；`state.eventHitboxes` 记录每个标签的点击区域，`init()` 里新增 canvas click/mousemove 监听——点事件标签播放头跳到那一天（复用 `setDay()`），侧栏节点卡自动讲当天状态。
- **图内标注瘦身**：缺货带/压仓块的件数+金额文字移到侧栏读数面板（本来就有「缺货情况」「少赚金额」「压仓占用」三格），图上只留「缺货 N 天」「压仓 N 件」短锚点；促销开始/售罄日期文字、补货箭头的「+N」和「提前期」文字整段删除（事件轨已覆盖）；预测覆盖差距文字删除（侧栏读数已有同口径文案，v0.11 就加过了）。
- **通用避让兜底**：新增 `placeLabels(ctx, items, minY, maxY)`——收集缺货带/压仓块/斜率标签/安全线标签/预测计划标签这几个还会互相打架的图内文字，贪心两两检测包围盒，撞了就顺延（优先下移，会撞 x 轴日期文字就改上移，两头都出界才夹住容忍轻微重叠）。新增 `clampLabelY()` 处理单个标签贴边溢出（撞事件轨顶部/x 轴底部/缺货带压仓块固定区）。压力测试过程：demand×forecast×promo 全组合 112 种 + 7 关 baseline，从最初 4 处失败（安全线撞缺货带、斜率标签撞 x 轴、压仓标签撞 x 轴）逐一定位修到 119/119 全过。
- **预测计划线标签定位算法**：不再固定挂在最右端（会撞压仓块）或固定中段（会撞缺货带），改成在「预测/真实两线差距最大」且「还没跌进底部拥挤区」的那一天动态取点——顺带更符合直觉：标签就出现在两线分得最开、最值得看的地方。
- **测试基建修复**（v0.12 顺带发现的真 bug）：`verify_lab.js` 的 `draw()` 因为 jsdom 没有真实排版引擎，`clientWidth`/`clientHeight` 恒为 0 又没有二次兜底，`resizeCanvas()` 只在 `onShow()` 里调用而测试从没调过——`draw()` 每次都在第一行 `if(!W||!H) return`，画布绘制代码（含这次新增的事件轨/避让算法）**实际上从 v0.10 起就零覆盖**。改用真实对象替换 Proxy mock（Proxy 的 `set` 陷阱丢弃所有写入，`ctx.textAlign` 读回来永远是 `undefined`，没法判断文字对齐方式算包围盒），给 `HTMLElement.prototype.clientWidth/clientHeight` 打桩固定视口尺寸，`draw()` 终于能被真正执行。

**验证**：
- 浏览器实测：`niceTop()` 贴合效果、坐标轴冻结（拖 demand 不变、库存冲顶时只涨不缩）、事件轨横向避让（4 个事件单行不重叠）、点击事件跳转、119 组合参数压力测试零重叠——过程中修了 3 轮真实边界 bug（预测计划标签固定位置撞压仓块 / 安全天数小时撞缺货带 / 促销斜率标签撞 x 轴日期 / 压仓块标签本身贴边撞 x 轴）。
- `verify_lab.js` 扩到 **73 项断言**，新增 draw() 真实覆盖确认、7 关零重叠、促销关 5 组极端参数零重叠、画布利用率 ≥50%、坐标轴冻结（拖参数不变/冲顶只涨不缩）、事件轨数量与点击跳转，全过。

遗留：教学训练 Tab「先学后练」重构仍未开始；`sku-simulator-mvp.html` 废弃单文件版仍未清理。

### v0.11.0 - 2026-07-26

状态：打通 Forecast 因果链。用户看第 6 关（当时是"预测"）问"我预测失败了，少赚钱怎么没体现"，追代码发现问题比表象更深——`forecast` 滑块从头到尾不影响任何结果（下单时机判断一直用真实日销算），是个纯装饰。用户同时指出界面还要再省空间（Tab 占一整行、滑块说明文案占一整行）。

**根因**：`lab-core.js` 的 `simulate()` 里补货触发判断 `cover = inv[t-1]/(p.demand||1)` 用的是真实日销，等于假设你有上帝视角，事先就知道明天能卖多少。现实里 Forecast 的全部意义恰恰是「你不知道真实需求，只能凭预测做决策」。

**改动**：
- `simulate()`/`computeSeries()` 重写：新增 `forecastDaily = hasForecastDriven ? p.forecast : p.demand`，决策侧（下单时机、下单量）永远用它算，结果侧（真实消耗）永远用 `p.demand` 算。`hasForecastDriven` 由 `hasElement(si,"forecast")` 判定——前 5 关该值为 false，`forecastDaily` 等于 `p.demand`，行为与 v0.10 完全一致（用测试验证了"还原成 forecast=demand 后与首次结果一致"，非漂移）；到 Forecast 关起才真正等于 `p.forecast`。
- `STAGES` 重排：坐标系→日销→售罄→**补货→安全**→**预测（大 boss）**→促销（原顺序是坐标系→日销→售罄→预测→补货→安全→促销）。前 5 关 baseline 统一 `forecast===demand`，第 6 关（Forecast）的 baseline 特意调小到 `inventory:5000, leadTime:30`（而不是沿用前面的 8000/140）——否则单次补货模型里，140 天提前期 + 180 天演示窗口根本放不下一整个"下单→到货→卖完"的周期，即使预测完全准确也会产生大量虚假压仓/断货，掩盖了预测误差本身的效果。
- 全文件搜索替换硬编码的 `si >= N` / `si === N`（十几处，分布在 `computeSeries`/`selloutMoment`/`draw`/`computeEvents`/`updateReadout`/`LEGEND[].when`），改成 `hasElement(si, name)`（累计判断）或 `si === stageIndexOf(name)`（仅当前关判断）。新增 `unlockedParams()` 的 `supersedes` 机制：Forecast 关起 `replenish` 滑块从 `newParam` 集合里被移除，改成用 `suggestedQty = forecastDaily*(HORIZON-arrivalDay)` 自动算（这个公式也调了两版：第一版 `forecastDaily*(safetyDays+leadTime)` 会因为"订单本来就是在库存刚跌破警戒线那一刻触发的"而使订购量趋近于 0，完全看不出预测越高订得越多；改成按"到货后需要撑到演示结束"来算，预测准时接近 0 成本，预测错时误差被放大成显著的少赚/压仓数字）。
- 金额换算：单件毛利/成本口径取自 `D:\Desktop\Govee\利润表.xlsx`（EU 原价 €79.99，采购+头程+尾程成本约 31，净利率 12%~25%），在 `lab-core.js` 内部独立定义 `UNIT_PRICE=79/UNIT_COST=31`（不依赖 index.html 的全局，因为 lab-core.js 是独立 IIFE，`verify_lab.js` 的 jsdom 环境也不加载 index.html 内联 script）。新增 `lostMoney`/`overstockMoney` 指标，图上补货缺口标注金额、期末剩货画灰蓝压仓块 + 金额。
- 修 v0.10 引入的一个真实回归：`--header-h` 由 `ResizeObserver` 测量，但在过渡态测到一次偏高值（343.5px，实际只有 63px）后不再更新，导致讲解区 `.lab-scroll` 被压到只剩 26px 高。改用纯 CSS flex（新增 `.app.lab-mode`，`switchTab("lab")` 时切换该 class），高度由浏览器自己算，彻底删除 `setupHeaderHeightVar()`。
- 顶栏收窄：`teach-core.js` 的 tabbar 从 `header.parentNode.insertBefore(bar, header.nextSibling)`（独立一行）改成 `header.insertBefore(bar, actions)`（并入品牌行，flex order 排布），省下一整行高度。滑块 `.lab-note` 文案从独立一行改成 `.lab-note-inline` 并入 `.lab-field-top` 标签行。
- 本地 `python -m http.server` 不发 `Cache-Control` 头，浏览器会缓存旧 JS，调试时"改了没生效"排查了很久——给三个 `<script src>` 加了 `?v=` 版本串，每次改 JS 记得同步更新。

**验证**：
- `verify_lab.js` 改用 `_debug.applyStage(idxOf(name))` 按元素名直接跳关（不再靠点击次数推断位置，重排后旧写法会脆弱失效），扩到 **64 项断言**，全过。新增专项：forecast 改变时 orderDay/lostMoney/overstockMoney 真的会变、预测偏低单调增少赚、预测偏高单调增压仓、还原到 forecast=demand 后结果与首次一致（非漂移）。
- 浏览器实测：forecast=48(准)/20(低估)/120(高估) 三档对比，orderDay 62→87→1，lostMoney 0→132480→0，overstockMoney 0→0→441440，方向和量级符合预期。
- 1280×800 视口下 7 关逐一截图确认布局不溢出（stage/actions 底部均 <800px）。

遗留：`sku-simulator-mvp.html` 仍是废弃单文件版，未清理；教学训练 Tab 尚未按"先学后练"重构（用户原计划的 Phase 2，本轮聚焦在动画实验室的 Forecast 因果修复上）。

### v0.10.0 - 2026-07-26

状态：动画实验室体验重构。用户用真实截图指出 v0.9.3 的坐标轴设计从根上算错了（日销翻倍，屏幕斜率反而变平），并列了一整份体验问题清单（布局被切、无解析、无撤销、教学训练太乱）。本次是 Phase 1（实验室部分），Phase 2（教学训练重构）与 Phase 3（Govee 真实数据导入）见计划文档。

**根因诊断**（先用浏览器实测复现，再动手改）：横轴按「覆盖天数」动态收窄 + 纵轴被负库存尾巴撑开，两层自动缩放叠加抵消了斜率变化。实测数据：demand=26→60 时旧版屏幕斜率 -1.108→-0.537（反而变平），售罄点几乎钉死在右边缘。

**改动**：
- `lab-core.js` `draw()`：坐标轴改为固定域——横轴恒 180 天，纵轴恒按 `max(12000, 期初库存+补货量)`（不受日销/负库存影响）；y 轴刻度改用"最高一档刻度"反推 yMax，避免刻度标签与像素位置不一致。修复后 demand=26→34→60 时屏幕斜率 -0.201→-0.263→-0.463 单调变陡、售罄点 100%→78.4%→44.4% 单调左移、三角形面积单调变小。
- `computeSeries`/`simulate()`：库存 clamp 到 0，不再画负库存；新增 `lostSales`/`stockoutDays` 统计「本来卖得掉却没货」的部分；红色区域改画成 0 线上方的「缺货带」+「少卖约 M 件」标注。
- 统一售罄日算法：`selloutMoment()` 第 4 关前用连续公式（库存/日销），第 4 关起（有提前期/促销）用离散模拟结果——红线位置和读数「库存覆盖」永远来自同一个数。
- 新增侧栏「📍 现在图上发生了什么」节点卡（`computeEvents`/`renderNodeCard`），跟着播放头/参数实时讲当前节点；第 3~7 关图上补齐直接标注（售罄公式、预测覆盖差距、提前期天数、下单节点圆点、促销斜率放大倍数）。
- `renderElements()` 反剧透：未解锁关卡 chip 只显示 🔒。
- 新增撤销/重做（Ctrl+Z/Ctrl+Shift+Z，`state.history`/`redoStack`，连续拖同一滑块合并成一条记录，切换滑块立刻算新一步）、「← 上一步」按钮、播放条（暂停/继续 + 拖动进度条 + 逐天步进）。
- 修「一键模拟」`autoDemo()`：演示前存快照，演示后恢复用户自己调过的值，不再覆盖成 baseline。
- `index.html` CSS：`#labView` 高度按 `ResizeObserver` 实测的顶栏高度动态算 `calc(100vh - var(--header-h))`；左侧舞台 flex 竖排钉死不滚动；右侧栏滑块移到顶部固定区，讲解卡片改 `<details>` 可折叠；`renderLegend()` 加缓存，只在画布元素集合变化时重建 DOM（原来拖滑块每帧重排整个图例）。

**验证**：
- 浏览器实测：demand 26/34/60 下屏幕斜率、售罄点位置、三角形面积三件套单调性（见上）；1280×800 视口下舞台全程在视口内（stage bottom=790 < 800）；一键模拟同步 rAF 验证参数正确恢复为用户值。
- `verify_lab.js`（`C:\Users\23805\WorkBuddy\2026-07-24-00-53-48\.workbuddy\research\verify_lab.js`）扩到 **75 项断言**，新增坐标轴单调性、库存非负、反剧透、撤销/重做，全过。
- 修复过程中发现并修正一个新引入的 bug：debounce 用了全局 `dragTimer`，快速切换不同滑块会被误合并成一条撤销记录——改成按滑块名（`dragParam`）判断是否为新一次操作。

遗留（下一步）：Phase 2 教学训练重构「先学后练」、Phase 3 Govee 真实数据导入；stage 5/6 多个标注同时出现时文字有轻微重叠，属已知的次要视觉打磨项。

### v0.9.0 - 2026-07-26

状态：侧栏结构化 + 动态图例（用户反馈"右边布局乱、没说每个元素是干嘛的、说明完全无关"）。

- 侧栏改分区卡片：📚 本关学啥 → 🧩 图上现在有什么 → 🎮 怎么拖 / 👀 看什么变化 → 📖 几何词典。
- 动态图例：只列画布上此刻真实渲染的元素（条件谓词与 draw() 一一对应），每条三段式（图里啥样 / 为啥这样画 / 意味着），拖滑块实时增减。
- 修核心 bug：第 6 关安全库存线之前 draw() 里根本没有渲染分支——文案在讲一条不存在的线。补琥珀横虚线（高度 = 安全天数 × 日销）。
- baseline 调值：第 2 关日销 8→27（三角形闭合）、第 6 关安全天数 7→14、第 7 关促销 1→1.4（尖峰默认可见）。
- 「看演示」= 滑块动画 → 播放头扫描 → 自动恢复 baseline 的完整教学动画。
- 修 syncSliders/demoStage 切关空引用报错；烟雾测试 64 项全过。

### v0.8.1 - 2026-07-26

状态：修两个新手最痛的反馈——"拖库存线没动" + "一键模拟没看到完整动画"。

变更：

- **draw：第 1 关 yMax 固定为 12000（不再随 inventory 走）**。第 1 关 STAGES[0].baseline.inventory=4800→3000、demoTarget.inventory=8000→9000；intro 加一句"y 轴刻度固定，线会在这些刻度间上下走"。效果：拖 0→12000 时线在固定坐标里从底部升到顶部，移动幅度大且明显。
- **y 轴刻度系统重构**：新增 `makeTicks(yMax, n)` 自动选 4 档"好看"刻度；第 1 关强制 5 档固定 [0, 3000, 6000, 9000, 12000]；其他关动态适配。画布左侧画多个 y 刻度 + 横向浅网格线，库存量级一眼可读。
- **新增 `autoDemo()` 替换原 `play()`**："一键模拟"现在做三件事：
  1. 立即把本关关键参数设到 `demoTarget`（库存线/斜率/三角形/箭头等瞬间到位）；
  2. 播放头从今天扫到 180 天后（2.4 秒 rAF 动画）；
  3. 演示完恢复 baseline，方便继续手动拖。
  - 第 1 关：看库存线从 3000 升到 9000 → 时间扫描（库存不变）→ 回到 3000；
  - 第 2 关：看日销从 8 升到 32 → 斜率变陡 + 三角形扩大 → 回到 8；
  - 第 3 关：看日销从 32 升到 64 → 红竖线往左移 → 回到 32；
  - 第 4-7 关同理（每关聚焦一个关键变化）。
- **verify_lab.js 升到 46 项**：新增「拖库存值变化」「一键模拟后回到 baseline」断言。

### v0.8.0 - 2026-07-26

状态：动画实验室从「搭积木」再升级为「看图说话」教学法（用户反馈：完全没看懂——为啥日均销量是直线、三角形面积是啥、为啥用竖线/虚线/箭头）

变更：

- **lab-core.js 重写文案**（v0.7→v0.8）：每关 intro/instruct/observe 全部重写为「先认概念 → 为啥用这个几何形状 → 怎么动 → 代表什么」三段式：
  - 第 1 关：从「坐标图」概念讲起——横=时间(天)、竖=库存(件)、横线高度=几件；
  - 第 2 关：解释「斜率 = -日销」「三角形面积 = 卖出的总数 = 初始库存」「绿色填充 = 有货覆盖区」；
  - 第 3 关：解释「撞到 0 = 售罄日」「为啥用红竖线（精确标那一天的位置）」「覆盖天数 = 库存÷日销」；
  - 第 4 关：解释「为啥用虚线（还没发生=计划）」「双线分开 = 决策错」；
  - 第 5 关：解释「箭头 = 到货日 = 下单日+提前期」「红缺货区 = 售罄到到货之间的零货天数」；
  - 第 6 关：解释「琥珀线高度 = 安全天数×日销」「只把下单提前，不延长物流」；
  - 第 7 关：解释「斜率临时放大 = 尖峰」「为啥用竖虚线标促销开始」。
- **draw 改造**：
  - 时间轴标签 `D0/D60/D120/D180` 全部改中文「今天 / 60 天后 / 120 天后 / 180 天后」；
  - 加 y 轴刻度（左边的「N 件」+「0」标签）；
  - 库存线 lineWidth 2→2.4 更明显；
  - 第 1 关去掉绿色填充避免遮挡，让横线一眼可见，并加「━ 这是一条横线，高度=你今天有 N 件货」大字标。
- **updateReadout 文案中文化**：`D{N}天` → `第 N+1 天`，售罄日补一句「还能撑 N 天」。
- **新增「📖 看图词典」侧栏面板**（`#labDict` + CSS `.lab-dict*`）：随关累计 12+ 条术语，每条=「术语 + 解释为啥用这个图形」，每关切关时刷新。
- **verify_lab.js 升级到 44 项断言**：7 关每关都验"intro 解释 X" + "词典有 X"，外加读数面板"不再出现裸 D{n}"、拖动联动、演示不报错。

### v0.7.0 - 2026-07-26

状态：动画实验室从「一次摊开所有元素」重做为「搭积木式逐关解锁」教学法（用户反馈原版太抽象、看半天看不懂）

变更：

- **lab-core.js 重写**：`STAGES` 取代原 `LESSONS`，每关只新增【一个视觉元素】+【一个因子滑块】，元素与滑块都按关累计解锁（`unlockedElements` / `unlockedParams`）。`computeSeries` 按当前关 gate（提前期≥第5关、促销≥第7关、预测线≥第4关才计算，避免提前出现未学的概念）。
- **draw 分阶段渲染**：第 1 关只画一条静止的库存横线 + 「📦 库存 N 件——还没开始卖」大字；第 2 关起才画下降曲线；售罄线(第3关)、预测虚线(第4关)、补货箭头(第5关)、安全库存线(第6关)、促销尖峰(第7关)逐关叠加。
- **侧栏新增「已解锁元素」进度条**（`#labElements` chip 列表，done/current 状态）+ 本关导语 `#labIntro` 区块；每关新滑块带「新」角标并高亮。
- **读数面板按关显示相关指标**：库存→+日销/覆盖→+售罄日→+预测覆盖→+下单/到货/缺货→+促销期日销，避免一次暴露过多数字。
- **测试**：`verify_lab.js` 升到 19 项断言（7 chip 渲染、各关滑块数量递进 1→2→2→3→5→6→8、chip 高亮/完成态、演示拖动、课程切换、播放不报错），全过。

### v0.6.0 - 2026-07-26

状态：新增「🎬 动画实验室」Tab——Forecast 可视化教学（库存消失 / 卖完 / 超卖 / 补货 / 安全库存 / 促销 / Q4 的动画与引导式拖动学习）

变更：

- **新增第三个 Tab「动画实验室」**：在 `teach-core.js` 的 `switchTab` 改为三态（teach / sandbox / lab），`init` 注入 `tabLab` 按钮与点击事件；`index.html` 在 `</main>` 后新增 `<section id="labView" hidden>` 占位，`<script src="lab-core.js">` 在底部加载。
- **库存曲线动画引擎（`lab-core.js`）**：`computeSeries` 算 180 天库存序列、售罄日、缺货天数、下单日、到货日、计划/真实覆盖天数；Canvas 绘制绿=有货、红=超卖区、琥珀虚线=安全库存线、绿上箭头=补货到货、红竖线=售罄日、紫色播放头；「一键模拟」用 `requestAnimationFrame` 逐天播放；`resizeCanvas` 用 devicePixelRatio 适配并支持主题变量读取。
- **八参数可拖动联动**：预测/真实日均销量、期初库存、补货提前期、安全库存天数、补货量、促销系数、促销开始日；拖动即时重绘并更新读数面板（售罄日/缺货天数/下单·到货日/计划·真实覆盖/期末库存）。
- **引导式分步教学（7 课）**：库存消失 / 预测≠真实 / 超卖 / 补货 Lead Time / 安全库存 / 促销尖峰 / Q4 大胆预测；每课设基线参数、只高亮该拖的滑块、给「从哪拖到哪」指令 + 观察提示 + 引用锚点（指标-/攻略P-）、配「看演示」自动拖动；「下一步」循环切换。
- **测试**：新增 `verify_lab.js`（jsdom 烟雾，9 项断言：8 滑块生成、7 课切换、拖动联动、演示/重置无报错）；`node --check` 通过 `lab-core.js` / `teach-core.js`。

遗留 / 下一步：

- 令牌推送：GitHub 细粒度 PAT 当前为只读（Contents=Read），`git push` 仍 403；需用户在 GitHub 把该令牌 Contents 改为 Read and write 后我方可自动推送（沙箱凭据已存于 `~/.git-credentials`）。
- 可选 Module D（导入真实 Govee xlsx 动态生成预设）与 Module E（周报/数据条工具网页化）仍待排期。

### v0.5.0 - 2026-07-24

状态：教学 / 场景 / 指标全面真实化（基于《独立站运营资料》+ Govee 真实周报数据，v0.5 的地基版本）

变更：

- **教学章节全面真实化（Module A）**：重写 `teach-data.js` 全部 11 章，嵌入 Govee 真实方法论与引用锚点——`forecast 攻略 P1–P8`（线下先行→ERP / 高库存拉高预测 / 新品从上市日 / 走物流拉到货 / 承压加 AS / Q4 大胆敢预测 / 畅销品预测到清完日）、`业务逻辑.jpg`（首单 SOP、目标正推倒推）、`指标逻辑.jpg`（GMV=CR×AOV×Session、成本五口子、ROI=销售额/广告花费）；
- **真实场景预设（Module B）**：自由沙盘预设从 3 个扩到 5 个——`eu-stable`(EU 稳定) / `uk-bf`(UK 黑五) / `new-launch`(新品首单) / `eol-clear`(EOL 清库) / `promo-defend`(促销防守)；数值区间锚定 Govee 真实日销分布（UK 30 天日均中位数 32、range 1–871；EU 30 天日均中位数 46、n=297；UK 含黑五日均 / PFD 日均 sheet；EU+UK 各 1105 MSKU）；
- **完整指标体系（Module C）**：沙盘新增 `v0.5 经营进阶` 四大面板（HTML + `enrichResult()` + `renderV5()` + CSS）：
  1. GMV 拆解：GMV=CR×AOV×Session，AOV=单价×连单率，Session 六路流量（Google/FB/AFF/Bing/Organic/Direct）占比条形可视化；
  2. 成本结构（指标逻辑五口子）：采购价/GMV、头程（多海运少空运）、尾程（派等单）、营销（AS 控 ROI）、其它（仓储/平台/风控/关税）、收单，按 GMV 占比估算并与贡献利润并列交叉验证；
  3. ROI 与复购：ROI=销售额÷广告花费（P6 该涨就涨但要盯 ROI）；复购率=积分/EDM/售后流量贡献的长期 GMV；
  4. 综合经营评分：GMV 25% + 贡献利润 25% + 库存健康 20% + 缺货控制 15% + Forecast 准确 10% + 价格健康 5%（handoff 第 13 节六维加权公式），六维条 + 总评颜色分级；
- 预设体系升级：新增 `aovMultiplier`(连单率)、`repurchaseRate`(复购率) 两个滑块输入 + `v5` 配置（traffic 六路占比 / cr / costRates 五口子费率 / note）；`currentPresetName` 跟踪当前场景用于面板标签；
- 烟雾测试升级：新增 `verify_v05.js`（54 项断言），覆盖 5 预设切换、四大面板渲染、无运行时错误。

验证：

- `verify_v05.js`（jsdom 真实 DOM）：app 初始化成功、5 个预设逐个切换无抛错、GMV/Session/成本合计/ROI/复购 GMV/评分标签均被填充（非 `—`）、成本行=6/评分行=6/流量图例=6、无 jsdom 运行时错误；结果 **54 passed, 0 failed**；
- 评分合理性抽查：eol-clear 综合 47.6 分 < promo-defend 83.3 分，符合预期；
- `node --check` 对 `index.html` 内联脚本与 `teach-data.js` 语法检查通过（测试以真实文件加载，等价于运行期校验）。

Git：

- 本地提交：v0.5.0 教学/场景/指标全面真实化；
- 同步：CloudStudio 重新部署为公开可访问 URL；GitHub 经 `git push origin master:main`（沙箱凭据可用，非交互推送）。

遗留问题 / 下一步：

- 真实浏览器视觉与交互验收仍未完成（沙箱无法打开本地文件浏览器）；建议本机用 1440×900 / 820×1180(iPad) / 390×844(手机) 三档目检一次 v0.5 新面板；
- **Module D（导入真实 Govee xlsx 进模拟器，动态生成预设/对比）** 与 **Module E（把 weekly_report_calculator / 手动弄数据条 两个 .exe 工具网页化，每周复用）** 已规划，依赖本版真实指标体系地基，待后续排期；
- 180 天时间口径、库存耗尽节点语义、图表边界等已知风险仍待处理（见 README 接手风险 1/2/4）。

### v0.4.0 - 2026-07-25

状态：从「纯模拟器」升级为「判断训练系统」（产品差异化核心）

变更：

- 顶部 Tab「教学训练 / 自由沙盘」切换；原 `<main>` 模拟器标为 `sandboxView`，新建 `teachView` 容器承载教学，保留 v0.3 全部计算引擎 / 响应式 / 浅深主题 / 真实备货公式面板不动；
- 新增 `teach-data.js`（11 章数据：c1-c8 必修 + a1-a3 选学）与 `teach-core.js`（教学引擎 IIFE）：首页 hero + 经验本条 + 必修/选学卡片网格 + 沙盘 CTA；
- 每章统一六步经验循环：① 观察数据并勾选要点 → ② 下判断（数字类滑杆 / 决策类单选）→ ③ 承诺理由 + 信心 → ④ 看系统模拟结果 → ⑤ 复盘归因 → ⑥ 提炼一条个人规则；
- Forecast 经验本（localStorage，key `sku_forecast_journal_v1`）：首页展示已完成循环 / 高估 / 低估 / 较准确 / 最常犯错根因；同章重做覆盖旧记录；
- 四维度评分：数据观察(obs) / 逻辑依据(logic) / 数值偏差(dev) / 信心校准(conf)，不看对错看判断质量；
- 沙盘解锁：完成 ≥3 次完整循环才解锁自由沙盘，否则显示遮罩与「还需 N 次」提示；
- 章节重排（按用户反馈）：必修 c1 Forecast 是什么 / c2 Baseline / c3 未来变化 Checklist / c4 分段 Forecast / c5 库存覆盖与时间轴 / c6 LT 与安全库存警戒线（拆分）/ c7 补货量与经营动作 / c8 预测偏差与经验；选学 a1 新品 / a2 EOL 清库 / a3 真实周报复盘；
- 业务口径严格对齐《销售预测培训》：平均日销 ERP 加权 = 3天×20%+7天×50%+15天×20%+30天×10%；客观全流程 Lead Time = 采购+提货+运输+清关尾程+上架（不含备货间隔、不含安全库存）；补货警戒覆盖期 = 客观 LT + 备货间隔 + 安全库存天数；**修正触发日公式**为「触发补货 = 库存覆盖天数 ≤ 客观 LT + 安全库存天数」（v0.2 漏算 LT）；
- 主题变量别名：`--paper/--panel/--ink-soft/--accent/--accent-2/--accent-soft` 在 `:root` 与 `[data-theme=dark]` 各加映射；
- 同步 `sku-simulator-mvp.html` 与 `index.html` 为同一份 v0.4 内容（含 teach 注入）。

验证：

- jsdom 真实 DOM 烟雾测试（`verify3.js`）：首页渲染 11 卡片 / 初始 0 循环 / 7 必修锁定；走通 c1/c2/c3/c4 数字类 + c5/a3 决策类经验循环；经验本 localStorage 写入正确；c2 在 c1 后解锁、c5 在 c4 后解锁；3 循环后沙盘解锁（无遮罩）；四维度评分格 = 4。结果 SMOKE_PASS；
- `node --check` 对 `teach-data.js` / `teach-core.js` 语法检查通过；
- 注入 CSS 与 :root 别名交叉核对完成。

Git：

- 待本地提交：v0.4.0 判断训练系统（本地 `master` 分支）；
- 同步：经 CloudStudio 重新部署为公开可访问 URL；GitHub 同步待本机 `git push origin master:main`（沙箱无写凭据）。

遗留问题：

- 真实浏览器视觉与交互验收仍未完成（沙箱无法打开本地文件浏览器）；建议本机用 1440×900 / 820×1180(iPad) / 390×844(手机) 三档目检一次；
- 180 天时间口径、库存耗尽节点语义、图表边界等已知风险仍待处理（见 README 接手风险 1/2/4）；
- 经验本目前仅本地 localStorage，未做跨设备 / 导出，后续可考虑「导出我的成长轨迹」。

### v0.3.0 - 2026-07-25

状态：响应式 UI 重构（移动优先 + 浅深主题）

变更：

- 重写 `<style>` 整块为移动优先响应式：桌面（≥1100px）/ iPad（768–1099px）/ 手机（≤767px）三档断点，iPad 宽度充分利用为双列卡片；
- 融合两项目长处：借「数学实验室」的友好章节卡片 + 渐变标题 + 进度条质感；借「谷歌推广模拟器」的 Material 卡片 / KPI 网格 / 状态 pill / 浅深主题切换（CSS 变量 + localStorage 记忆 + 浮动切换按钮）；
- 触控友好：最小点击目标 44px、滑块加大、移动端底部固定「重新模拟」按钮；
- 图表「补货到达」线 / 文字改用 CSS 变量接管，深色模式下不再糊成背景色；
- 保留 v0.2 全部计算引擎与「真实备货公式」面板逻辑不动，仅改样式与注入主题切换；
- 同步 `sku-simulator-mvp.html` 与 `index.html` 为同一份 v0.3 内容。

验证：

- DOM 契约交叉检查：脚本引用的 53 个 getElementById ID 在 HTML 中全部存在，无遗漏；
- CSS 括号配平：159 / 159；
- 引擎烟雾测试（DOM 桩）：三预设均正常渲染、真实备货公式面板字段均被填充，SMOKE_PASS；
- 在线部署验收：`https://5afbd39aaa104900a4cfeb8a06d8b486.app.codebuddy.work` 返回 200，且含 `themeToggle` / `data-theme="dark"` / `color-scheme` / 预设按钮 / 真实备货公式 等 v0.3 特征标记。

Git：

- 本地提交：v0.3.0 响应式 UI 重构（本地 `master` 分支）；
- 同步：经 CloudStudio 部署为公开可访问 URL；GitHub 同步待本地 `git push`（沙箱无写凭据，需在本机执行 `git push origin master:main`）。

遗留问题：

- 真实浏览器视觉与交互验收仍未完成（沙箱无法打开本地文件浏览器）；建议在你本机用 1440×900 / 820×1180(iPad) / 390×844(手机) 三档目检一次；
- 180 天时间口径、库存耗尽节点语义、图表边界等已知风险仍待处理（见 README 接手风险 1/2/4）。

### v0.2.0 - 2026-07-25

状态：对齐《销售预测培训》业务公式

变更：

- 全流程 Lead Time 对齐培训口径：备货间隔 14 + 采购 45 + 提货 0 + 物流 60 + 上架 0 = 119 天（原模型漏算备货间隔）；
- 新增「真实备货公式」面板：平均日销（3/7/15/30 天加权）、当天备货量、红绿线、补货触发日（智能发货逻辑）；
- 三预设调参形成教学对比：稳定款=良好 / 大促款=危险（保守预测致第 116 天断货、第 88 天应补货）/ 积压款=警告（预测过高致 435 天覆盖积压）；
- 本地 `index.html` 与 GitHub 约定的 `sku-simulator-mvp.html` 同步为同一份 v0.2 内容；
- 合并线上独有文档 README.md 与 DEVELOPMENT_LOG.md 到本地开发副本。

验证：

- Node 抽取 simulate() 引擎跑三预设：stable=good / promo=danger / slow=warn，区分正确；
- `node --check` 全脚本语法检查通过；
- DOM 桩冒烟测试：三预设均正常渲染、新面板字段均被填充，无运行时异常。

Git：

- 本地提交：v0.2 对齐销售预测培训公式（feat 提交，本地 `master` 分支）；
- 同步：经 GitHub MCP 以新增提交方式推回远程 `main`，不覆盖 v0.1.1 历史（遵守“不强制推送”规则）。

遗留问题：

- 浏览器视觉与交互验收仍未完成（本环境无法打开本地文件浏览器）；
- 180 天时间口径、库存耗尽节点语义、图表边界等已知风险仍待处理（见 README 接手风险 1/2/4）；
- 模型与诊断仍耦合在单文件中，尚未抽离为可单测的纯函数。

### v0.1.1 - 2026-07-24

状态：文档与协作规范更新

变更：

- 明确完成改动后默认提交并同步到 GitHub；
- 建立 `MAJOR.MINOR.PATCH` 语义化版本规则；
- 建立 `main`、功能分支和跨设备同步规范；
- 新增本开发记录，确保任何接手者都能了解项目状态；
- 修正交接提示词中的实际 HTML 文件名。

验证：

- 检查 README、交接文档和开发记录中的仓库地址、版本号和命令；
- 确认本次没有修改业务模型与页面运行逻辑。

Git：

- 目标版本标签：`v0.1.1`
- 提交：本条记录对应的 `v0.1.1` 版本提交

遗留问题：

- 浏览器视觉与交互验收仍未完成；
- 模型和时间轴已知风险继续以 README 中的接手风险为准。

### v0.1.0 - 2026-07-24

状态：原始 MVP 基线

变更：

- 导入单文件 SKU 经营训练模拟器；
- 导入完整开发交接文档；
- 新增接手 README 和首轮静态检查结论；
- 创建 GitHub 私有仓库并推送 `main`。

验证：

- JavaScript 语法检查通过；
- 三个预设完成核心计算冒烟验证；
- 55 个 DOM ID 无重复且无缺失引用。

Git：

- 提交：`aec0276 Initial MVP handoff`
- 版本标签：`v0.1.0`

遗留问题：

- 180 天口径、库存耗尽节点、积压诊断和图表边界仍需处理；
- 自动化测试尚未建立。
